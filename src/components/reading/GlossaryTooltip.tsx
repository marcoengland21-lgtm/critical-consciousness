/**
 * @deprecated Chunk 3b piece 2b. The misnamed click-popover under this
 * file moved to GlossaryPopover.tsx. The hover tooltip is now part of
 * GlossaryInlineTerm via the <Tooltip> primitive.
 *
 * This file is a stub kept only because the bash sandbox can't unlink;
 * Mars deletes it on the next housekeeping commit (`git rm`). No
 * remaining imports point here.
 */
export {}
